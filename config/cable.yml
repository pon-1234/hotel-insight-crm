development:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>

test:
  adapter: test

production:
  adapter: redis
  # Fail fast in production when REDIS_URL is missing.
  url: <%= ENV.fetch("REDIS_URL") %>
  channel_prefix: lineinsight
