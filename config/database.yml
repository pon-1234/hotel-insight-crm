# MySQL. Versions 5.5.8 and up are supported.
#
# Install the MySQL driver
#   gem install mysql2
#
# Ensure the MySQL gem is defined in your Gemfile
#   gem 'mysql2'
#
# And be sure to use new-style password hashing:
#   https://dev.mysql.com/doc/refman/5.7/en/password-hashing.html
#
default: &default
  adapter: mysql2
  encoding: utf8mb4
  collation: utf8mb4_unicode_ci
  pool: <%= ENV.fetch("DB_POOL") { ENV.fetch("SIDEKIQ_CONCURRENCY") { ENV.fetch("RAILS_MAX_THREADS") { 5 } } } %>

development:
  <<: *default
  encoding: utf8mb4
  collation: utf8mb4_unicode_ci
  username: <%= ENV['DATABASE_USERNAME'] %>
  password: <%= ENV['DATABASE_PASSWORD'] %>
  database: <%= ENV['DATABASE_NAME'] %>
  host: <%= ENV.fetch("DATABASE_HOST", "localhost") %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  encoding: utf8mb4
  collation: utf8mb4_unicode_ci
  username: <%= ENV['DATABASE_USERNAME'] %>
  password: <%= ENV['DATABASE_PASSWORD'] %>
  database: <%= ENV['DATABASE_NAME'] %>_test
  host: <%= ENV.fetch("DATABASE_HOST", "localhost") %>

production:
  <<: *default
  encoding: utf8mb4
  collation: utf8mb4_unicode_ci
  username: <%= ENV['DATABASE_USERNAME'] %>
  password: <%= ENV['DATABASE_PASSWORD'] %>
  socket: <%= ENV['DATABASE_SOCKET'] if ENV['DATABASE_SOCKET'] && !ENV['DATABASE_SOCKET'].empty? %>
  host: <%= ENV['DATABASE_HOST'] if !ENV['DATABASE_SOCKET'] || ENV['DATABASE_SOCKET'].empty? %>
  database: <%= ENV['DATABASE_NAME'] %>
